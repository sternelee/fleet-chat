# Mention Feature Visual Guide

## User Interface Flow

### 1. Normal Search Input (No Mentions)
```
┌─────────────────────────────────────────────────────────┐
│  🔍  Search or ask AI... (Use @app and #file to mention)│
└─────────────────────────────────────────────────────────┘

Hints:  [@] Mention apps  [#] Mention files  [>] Plugins  [/] Files  [?] Everything
```

### 2. Typing Application Mention (@)
```
┌─────────────────────────────────────────────────────────┐
│  🔍  Open @chr|                                          │
└─────────────────────────────────────────────────────────┘
         ↓
    ┌──────────────────────────────────────────────┐
    │ @ Applications                               │
    │──────────────────────────────────────────────│
    │ → 🌐  Google Chrome                          │
    │   📍  /Applications/Google Chrome.app        │
    │                                              │
    │   🔵  Chromium                               │
    │   📍  /Applications/Chromium.app             │
    │                                              │
    │   📘  Chrome Remote Desktop                  │
    │   📍  /Applications/Chrome Remote...         │
    └──────────────────────────────────────────────┘
    ↑↓ to navigate • ↵ to select • esc to close
```

### 3. Typing File Mention (#)
```
┌─────────────────────────────────────────────────────────┐
│  🔍  Show me #package|                                   │
└─────────────────────────────────────────────────────────┘
         ↓
    ┌──────────────────────────────────────────────┐
    │ # Files                                      │
    │──────────────────────────────────────────────│
    │ → 📄  package.json                           │
    │   📍  /Users/you/project/package.json        │
    │                                              │
    │   📄  package-lock.json                      │
    │   📍  /Users/you/project/package-lock.json   │
    │                                              │
    │   📄  packages.txt                           │
    │   📍  /Users/you/documents/packages.txt      │
    └──────────────────────────────────────────────┘
    ↑↓ to navigate • ↵ to select • esc to close
```

### 4. Completed Mention
```
┌─────────────────────────────────────────────────────────┐
│  🔍  Open @chrome and show #README.md|                   │
└─────────────────────────────────────────────────────────┘

Parsed Mentions:
  • @chrome    → Google Chrome (/Applications/Google Chrome.app)
  • #README.md → /Users/you/project/README.md
```

### 5. AI Response with Context
```
┌─────────────────────────────────────────────────────────┐
│  💬  OpenAI Response                              [✕]   │
├─────────────────────────────────────────────────────────┤
│  Query: Open @chrome and show #README.md                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  I'll help you with that:                              │
│                                                         │
│  1. Opening Google Chrome                              │
│     Path: /Applications/Google Chrome.app              │
│                                                         │
│  2. Displaying README.md                               │
│     Path: /Users/you/project/README.md                 │
│                                                         │
│  The README file will open in your default markdown    │
│  viewer, and Chrome will launch in the background.     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## Feature Components

### Mention Parser
```
Input:  "Open @chrome and show #README.md"
        ↓
Parse:  Detect @ and # prefixes
        ↓
Extract: 
        {
          type: 'app',
          text: 'chrome',
          fullText: '@chrome',
          startIndex: 5,
          endIndex: 12
        }
        {
          type: 'file', 
          text: 'README.md',
          fullText: '#README.md',
          startIndex: 22,
          endIndex: 32
        }
```

### Suggestion Matcher
```
Query: "chr"
       ↓
Match: 
  ┌─────────────────────┐
  │ Exact Match         │
  │ name === "chr"      │ (Priority 1)
  └─────────────────────┘
         ↓ (no match)
  ┌─────────────────────┐
  │ Starts With         │
  │ name.startsWith()   │ (Priority 2)
  └─────────────────────┘
         ↓ (found!)
  Results:
    • Chrome ✓
    • Chromium ✓
         ↓
  ┌─────────────────────┐
  │ Contains            │
  │ name.includes()     │ (Priority 3)
  └─────────────────────┘
         ↓
  Additional:
    • Chrome Remote Desktop ✓
```

### Resolution Flow
```
Mention: @chrome
   ↓
Backend: search_app_suggestions("chrome", limit=5)
   ↓
Results: [
  { name: "Google Chrome", path: "/Applications/..." },
  { name: "Chromium", path: "/Applications/..." }
]
   ↓
Match:   Find exact or best match
   ↓
Resolve: Google Chrome selected
   ↓
Entity:  {
  name: "Google Chrome",
  path: "/Applications/Google Chrome.app",
  icon: "data:image/png;base64,..."
}
```

## State Machine

```
┌──────────────┐
│  Idle State  │
└──────┬───────┘
       │ User types '@' or '#'
       ↓
┌──────────────────┐
│ Detecting Mention │
└──────┬───────────┘
       │ Mention detected
       ↓
┌──────────────────┐
│ Fetching Results  │ ←──┐
└──────┬───────────┘    │
       │ Results ready  │ User continues typing
       ↓                │
┌──────────────────┐    │
│ Showing Dropdown  │────┘
└──────┬───────────┘
       │ User selects or closes
       ↓
┌──────────────────┐
│ Mention Complete  │
└──────┬───────────┘
       │ Back to normal input
       ↓
┌──────────────────┐
│  Idle State      │
└──────────────────┘
```

## Keyboard Navigation Map

```
┌─────────────────────────────────────────────┐
│             Search Input                    │
│  "Open @chr|"                               │
└─────────────────────────────────────────────┘
       ↓ Autocomplete visible
┌─────────────────────────────────────────────┐
│  [↑] - Move selection up                    │
│  [↓] - Move selection down                  │
│  [Enter] / [Tab] - Select highlighted       │
│  [Esc] - Close dropdown                     │
│  [Backspace] - Edit mention text            │
│  [Letter keys] - Continue typing            │
└─────────────────────────────────────────────┘
```

## Example Workflow Diagram

### Opening VSCode with a File

```
1. User Intent
   "Open VSCode and show package.json"
   
2. User Types
   ┌──────────────────────────┐
   │ Open @vs|                │  ← Autocomplete shows
   └──────────────────────────┘

3. Select VSCode
   ┌──────────────────────────┐
   │ Open @vscode and show    │  ← Mention completed
   └──────────────────────────┘

4. Continue Typing
   ┌──────────────────────────┐
   │ Open @vscode and show #p|│  ← Autocomplete shows files
   └──────────────────────────┘

5. Select File
   ┌──────────────────────────────────┐
   │ Open @vscode and show #package.json│  ← Both mentions done
   └──────────────────────────────────┘

6. Send to AI
   ┌─────────────────────────────────────────┐
   │ Context:                                │
   │  @vscode → Visual Studio Code           │
   │            /Applications/VSCode.app     │
   │  #package.json → /project/package.json  │
   └─────────────────────────────────────────┘

7. AI Executes
   ┌─────────────────────────────────────────┐
   │ ✓ Launching Visual Studio Code          │
   │ ✓ Opening package.json                  │
   │   File path: /project/package.json      │
   └─────────────────────────────────────────┘
```

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (Lit)                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────┐    ┌─────────────────────────────┐   │
│  │ Mention Parser   │───→│  Search Component           │   │
│  │ (mention-parser) │    │  (search.component.ts)      │   │
│  └──────────────────┘    └───────────┬─────────────────┘   │
│                                      │                     │
│  ┌──────────────────┐                │                     │
│  │ Autocomplete UI  │←───────────────┘                     │
│  │ (dropdown)       │                                      │
│  └──────────────────┘                                      │
│                                                             │
└────────────────────────┬────────────────────────────────────┘
                         │ Tauri IPC
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                       Backend (Rust)                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────────────────────────────────────────┐     │
│  │  Search Commands (search.rs)                      │     │
│  │                                                    │     │
│  │  • search_app_suggestions()                       │     │
│  │  • search_file_suggestions()                      │     │
│  │  • get_application_icon()                         │     │
│  └───────────────────────────────────────────────────┘     │
│                          ↓                                  │
│  ┌───────────────────────────────────────────────────┐     │
│  │  System APIs                                      │     │
│  │                                                    │     │
│  │  • Application List (applications crate)          │     │
│  │  • File System (std::fs, ignore crate)           │     │
│  │  • Icon Extraction (icns crate)                   │     │
│  └───────────────────────────────────────────────────┘     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Performance Profile

```
┌─────────────────────────────────────────────────────────────┐
│  Operation           │ Time     │ Notes                     │
├──────────────────────┼──────────┼──────────────────────────┤
│  Parse Input         │ <1ms     │ Regex matching            │
│  Detect Mention      │ <1ms     │ Cursor position check     │
│  Fetch Apps (cached) │ <5ms     │ In-memory cache           │
│  Fetch Apps (first)  │ ~100ms   │ Load from system          │
│  Fetch Files         │ 10-50ms  │ Filesystem walk           │
│  Render Dropdown     │ <5ms     │ Lit component render      │
│  Load Icon           │ 20-100ms │ Async, on-demand          │
│  Resolve Mention     │ 10-30ms  │ Backend lookup            │
│  AI Context Build    │ <5ms     │ String concatenation      │
└─────────────────────────────────────────────────────────────┘

Optimizations:
• App cache loaded once at startup
• Debounced API calls (50ms)
• Icon loading is async
• Results limited to 10 items
• File search respects .gitignore
```

## Error States

```
┌─────────────────────────────────────────────┐
│  No Matches                                 │
├─────────────────────────────────────────────┤
│  🔍  @nonexistent                           │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │ @ Applications                        │ │
│  │───────────────────────────────────────│ │
│  │ No applications found                 │ │
│  │                                       │ │
│  │ Try:                                  │ │
│  │ • Check spelling                      │ │
│  │ • Use partial name                    │ │
│  │ • Verify app is installed             │ │
│  └───────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

```
┌─────────────────────────────────────────────┐
│  Resolution Failed                          │
├─────────────────────────────────────────────┤
│  AI sees mention but can't resolve:         │
│                                             │
│  Context:                                   │
│  - Application "xyz" (not found)            │
│                                             │
│  AI Response:                               │
│  "I couldn't find the application 'xyz'.    │
│   Please verify the name and try again."    │
└─────────────────────────────────────────────┘
```

## Future UI Enhancements (Planned)

### Visual Highlighting
```
Current:
┌─────────────────────────────────────────────┐
│  Open @chrome and show #README.md           │
└─────────────────────────────────────────────┘

Planned:
┌─────────────────────────────────────────────┐
│  Open [@chrome] and show [#README.md]       │
│         ╰──────╯             ╰─────────╯    │
│         blue                 green          │
└─────────────────────────────────────────────┘
```

### Hover Tooltips
```
┌─────────────────────────────────────────────┐
│  Open [@chrome]← [Tooltip]                  │
│         │       ┌─────────────────────────┐ │
│         └──────→│ Google Chrome           │ │
│                 │ /Applications/...       │ │
│                 │ Version: 120.0          │ │
│                 └─────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### Mention Chips
```
Current:
Open @chrome and #README.md

Planned:
Open ┌─────────┐ and ┌───────────┐
     │@chrome  │     │#README.md │
     │    [×]  │     │    [×]    │
     └─────────┘     └───────────┘
```
